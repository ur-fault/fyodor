use std::{
    io::{self},
    iter,
};

use fyodor::{
    renderer::Renderer,
    ui::{popup::Popup, Window},
};

fn main() -> io::Result<()> {
    let mut renderer = Renderer::new()?;

    panic!("test");

    Ok(())
}
